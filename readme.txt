使用Python语言，安装好相应的库文件之后，需要将整个文件设置为包含，然后直接运行run.py；
test_nocam.py是测试没有摄像头功能的；
Raspberry_collectdata是为了区分PC端trainning_data设置的文件，在运行的时候是读取training_data文件的；
mlp_xml是训练出来的最终文件；
三张图片是对应对的界面按键，注意：需要修改路径；
mainwindow.py是由ui文件PyUic工具修改来的，其中函数进行添加即可。